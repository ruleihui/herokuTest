#wakeUp heroku
name: 'dailyCloneWakeUp'

on:
  workflow_dispatch:
  repository_dispatch:
  schedule:
    - cron: 0 1/8 * * *

jobs:
  get_points:
    runs-on: ubuntu-latest
    steps:
    - name: 'WakeUp'
      run: |
        curl https://testhreroks.herokuapp.com/
